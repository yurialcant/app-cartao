spring:
  profiles:
    active: local
  application:
    name: benefits-core
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/benefits_db
    username: benefits
    password: benefits_password
    pool:
      initial-size: 20
      max-size: 50
      max-idle-time: 30m
  data:
    r2dbc:
      repositories:
        enabled: true

server:
  port: 8091

logging:
  level:
    root: INFO
    com.benefits: DEBUG
    org.springframework.boot.autoconfigure: DEBUG
    org.springframework.web: DEBUG
