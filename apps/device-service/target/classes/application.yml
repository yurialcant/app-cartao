spring:
  profiles:
    active: local
  application:
    name: device-service
server:
  port: 8098
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
