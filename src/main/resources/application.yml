spring:
  profiles:
    active: local
  application:
    name: settlement-service
server:
  port: 8096
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
