spring:
  profiles:
    active: local
  application:
    name: support-service
server:
  port: 8095
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
