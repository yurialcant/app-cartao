spring:
  profiles:
    active: local
  application:
    name: audit-service
server:
  port: 8099
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
